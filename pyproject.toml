[build-system]
requires = ["hatchling>=1.24"]
build-backend = "hatchling.build"

[project]
name = "rfr-gapfill"
version = "0.1.0"
description = "Leakage-safe Random Forest Robust (RFR) gap filling for eddy-covariance flux data, after Zhu et al. (2022)."
readme = "README.md"
license = { file = "LICENSE" }
requires-python = ">=3.10"
authors = [{ name = "Paul Inkenbrandt", email = "paulinkenbrandt@gmail.com" }]
keywords = [
    "eddy covariance",
    "gap filling",
    "random forest",
    "FLUXNET",
    "carbon flux",
    "NEE",
]
classifiers = [
    "Development Status :: 2 - Pre-Alpha",
    "Intended Audience :: Science/Research",
    "License :: OSI Approved :: MIT License",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Programming Language :: Python :: 3.14",
    "Topic :: Scientific/Engineering :: Atmospheric Science",
    "Typing :: Typed",
]
dependencies = [
    "numpy>=1.24",
    "pandas>=2.0",
    "scipy>=1.10",
    "scikit-learn>=1.3",
    "joblib>=1.3",
]

[project.optional-dependencies]
dev = [
    "pytest>=8.0",
    "pytest-cov>=5.0",
    "ruff>=0.6",
    "mypy>=1.11",
    "pandas-stubs",
]
# Notebooks are deliberately NOT a core or dev requirement; the package core
# must never depend on them.
notebooks = ["jupyter", "matplotlib"]
# YAML configuration files for load_config and the CLI. JSON needs nothing extra.
yaml = ["pyyaml>=6.0"]
# Sphinx and the extensions docs/conf.py loads; Read the Docs installs this extra.
docs = [
    "sphinx>=8.0",
    "myst-nb>=1.1",
    "furo>=2024.8",
    "sphinx-copybutton>=0.5",
    "sphinx-argparse>=0.5",
]

[project.urls]
Homepage = "https://github.com/inkenbrandt/Imputation"
Documentation = "https://rfr-gapfill.readthedocs.io/"
Source = "https://github.com/inkenbrandt/Imputation"

[project.scripts]
rfr-gapfill = "rfrgapfill.cli:main"

[tool.hatch.build.targets.wheel]
packages = ["src/rfrgapfill"]

[tool.hatch.build.targets.sdist]
include = ["src", "tests", "docs", "examples", "README.md", "LICENSE", "CITATION.cff"]

[tool.pytest.ini_options]
minversion = "8.0"
testpaths = ["tests"]
addopts = "-ra --strict-markers --strict-config"
markers = [
    "slow: long-running tests (model fitting, end-to-end scenarios)",
    "fluxnet: requires real FLUXNET2015 input data not shipped with the package",
    "supplement: requires the journal supplementary files (mmc*.docx / mmc7.xlsx)",
    "notebooks: executes the example notebooks (needs the `notebooks` extra)",
]
filterwarnings = ["error::DeprecationWarning:rfrgapfill.*"]

[tool.ruff]
line-length = 100
src = ["src", "tests"]
# Markdown here is specification prose, not formattable source.
extend-exclude = ["*.md"]
target-version = "py310"

[tool.ruff.lint]
select = ["E", "F", "W", "I", "UP", "B", "SIM", "NPY", "PD", "RUF"]

[tool.ruff.lint.per-file-ignores]
"tests/*" = ["B018"]

[tool.mypy]
python_version = "3.10"
files = ["src/rfrgapfill"]
strict = true
warn_unreachable = true

[[tool.mypy.overrides]]
module = ["sklearn.*", "scipy.*", "joblib.*", "matplotlib.*", "yaml", "yaml.*"]
ignore_missing_imports = true

# numpy's bundled stubs use `type` statements, which mypy cannot parse while
# python_version targets 3.10. Skip them rather than lower the checked language
# level and stop catching syntax this package's floor does not support.
[[tool.mypy.overrides]]
module = ["numpy.*"]
follow_imports = "skip"
follow_imports_for_stubs = true

[tool.coverage.run]
source = ["rfrgapfill"]
branch = true

# CI fails a run whose branch coverage falls below this. It sits a few points
# under the measured total (94%) so platform noise cannot trip it, while a new
# module landing without tests can.
[tool.coverage.report]
fail_under = 90
show_missing = true
